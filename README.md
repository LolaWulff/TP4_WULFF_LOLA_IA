# TP4 – IA – Transformada de Hough (Rectas)

Prototipo propio (sin `cv2.HoughLines`) para la Actividad 2.

## Requisitos
- Python 3.9+
- `numpy`, `Pillow`, `matplotlib`

## Instalación
```bash
python -m venv .venv
# Activar entorno:
# Windows (PowerShell): .\.venv\Scripts\Activate.ps1
# Linux/Mac: source .venv/bin/activate

pip install --upgrade pip
pip install -r requirements.txt
pip install -e .
